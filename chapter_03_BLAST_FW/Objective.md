# Fetch the JIRA ID and create a Test Scenario Generator
# JIRA-ID is KAN-4 --> Fetch Test Scenario


//Prompt: Using files B.L.A.S.T.md,objective.md I want you to follow the Blast framework phase0 and phase1 and start asking me the questions which you want based on the objective.

//1. I want the input as Jira ID and create a UI using react with good design and also output using the test scenario generator. I have given the document of testscenario in the attachment of Jira ID. 2. I will provide Jira credentials like url,token and email and using groq API. 3. data is in Jira ID. 4. I need a UI created , so give me the URL and then I will use versel to upload it on versel. 5. Act as a 15 years automation engineer and provide proper code for UI in react to create the UI part and also the supporting documents to create start creating it. Ask me whatevver you need for Jira and groq

//I have added the other phases in B.L.A.S.T.md file continue updates as per the data. Ask me wherever you have questions and need the input

//Harness prompt:
You read the B.L.A.S.T.md file again and my objective again and create a lightweight with beatuful UI and technically good React application which will take: the jira configuration, jira email ID, jira token, my jira base URL, the connection to my openGPT 120 billion group API key, which is groq. You will be able to create a test strategy based on by fetching the KAN-4 automatically.

//Use the vercel CLI and push the project to the JiraTestStrategy.vercel.app and here is my token to push: YOUR_VERCEL_TOKEN